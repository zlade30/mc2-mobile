# Two Apple accounts are used — credentials are injected per lane via ENV vars in Fastfile.
# Default matches TestFlight/staging bundle; override with IOS_BUNDLE_ID when needed.
app_identifier("com.mc2.app.test")
